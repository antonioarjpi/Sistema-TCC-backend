package com.estacio.tcc.repository;

import com.estacio.tcc.model.TipoTcc;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoTccRepository extends JpaRepository<TipoTcc, Long> {
}
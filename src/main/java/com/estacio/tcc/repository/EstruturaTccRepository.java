package com.estacio.tcc.repository;

import com.estacio.tcc.model.EstruturaTcc;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EstruturaTccRepository extends JpaRepository<EstruturaTcc, Long> {
}